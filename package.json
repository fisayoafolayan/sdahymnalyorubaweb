{
  "name": "sda-hymnal-yoruba",
  "version": "1.0.0",
  "private": true,
  "description": "SDA Hymnal Yorùbá - a complete Yorùbá hymnal for Seventh-day Adventist worship",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch & npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.0.0"
  }
}
